# 强制评审只覆盖「改错了很难回退」的四类路径。其余改动 CI 绿即可自合。
#
# 为什么不给每个模块都配负责人：2-3 人团队里，逐模块强制评审的主要产出是等待，
# 不是质量。模块边界由 .importlinter 在 CI 里机器检查，比人肉评审更可靠也更快。
# 团队规模超过 5-6 人时再把模块负责人加回来（协作规范第 1 节有还原方式）。
#
# 占位符说明：把 @arch-owner 等替换为实际 Git 账号或团队名（如 @org/backend）。
# 后匹配的规则覆盖先匹配的，因此越具体的路径放在越下面。

# 需求基线：改动意味着验收口径变了，需产品负责人确认版本号变化
/docs/product/                  @product-owner
/docs/data/                     @product-owner

# 跨模块契约：改动的代价由消费方承担，所以消费方要有话语权（ADR-0004）
/contracts/                     @arch-owner

# 迁移：线上数据改错无法靠回滚代码挽回
/alembic/versions/              @engine-owner

# 分层依赖契约本身：放宽约束等于放宽全仓边界
/.importlinter                  @arch-owner

# 其余路径无强制 owner。想让人看时在 PR 里 @ 对方，或走 draft PR 讨论。
